const initialState = {
  contacts: {
    items: [],
  },
  filters: {
    name: "",
  },
};

export const contactsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
